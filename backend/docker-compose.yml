version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: compras
    ports: ["5455:5432"]
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
