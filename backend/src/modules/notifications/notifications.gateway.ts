/**
 * Este archivo ya no usa socket.io.
 * La comunicación en tiempo real se hace vía SSE en notificacion.service.ts
 * Se mantiene solo para no romper imports existentes — se puede eliminar más adelante.
 */
export {};
